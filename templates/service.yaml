apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-tiki-fe-deploy.fullname" . }}
  labels:
    {{- include "web-tiki-fe-deploy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-tiki-fe-deploy.selectorLabels" . | nindent 4 }}
